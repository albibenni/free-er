// GTK4 / Relm4 UI — Phase 5
// Stub only. Run `free-er-ui` to launch the settings window (not yet implemented).

fn main() {
    eprintln!("free-er UI not yet implemented (Phase 5)");
    std::process::exit(1);
}
